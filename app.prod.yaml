runtime: go124
service: default

# Conexión a Cloud SQL — prod
beta_settings:
  cloud_sql_instances: loopi-prod-497600:us-central1:loopi-db-prod

env_variables:
  ENV: prod
  GCP_PROJECT: loopi-prod-497600

automatic_scaling:
  min_instances: 1
  max_instances: 10
  target_cpu_utilization: 0.65
